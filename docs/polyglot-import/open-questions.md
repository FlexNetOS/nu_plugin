# Open Questions

- Should rust-analyzer SCIP/export remain a planning-only Tier 4 lane for V1.2,
  or should it receive a stronger contract in the first implementation wave?
- Should JS/TS planning standardize on Oxc-first, SWC-first, or a mixed parser
  strategy?
- Which parser/runtime combinations are acceptable on the default path when
  pure-Rust options are incomplete?
- How much of single-binary export should be treated as crate-generation versus
  archive-generation in the first implementation wave?
- Is a dedicated V1.2 PRD addendum preferred by repo policy, or should the
  planning package stay doc-first without a separate PRD artifact?
