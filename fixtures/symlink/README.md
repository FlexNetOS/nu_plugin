# Symlink Fixture

`symlink_manifest.txt` declares the symlink shape for tests that can create
links on the active platform. The source fixture stores the declaration as text
so archive and checkout behavior remains portable.
