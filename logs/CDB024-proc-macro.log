# TASK-CDB024 proc-macro static detection validation

## Scope and safety

- Approval: `APPROVAL-TASK-CDB024` (digest-bound owner authorization).
- Dependency: CDB022 is `complete` in `execution/TASK_GRAPH.csv`.
- Validation is an offline static-parser fixture. It does not execute a target
  proc macro, build script, or CodeDB dynamic capture path.
- Existing unrelated worktree changes were preserved. This log is the sole
  TASK-CDB024 modification.

## Validation command

```text
CARGO_NET_OFFLINE=true cargo test --locked -p codedb-rust-static proc_macro_fixture_emits_static_rows_and_gaps
```

## Result

```text
running 1 test
test tests::proc_macro_fixture_emits_static_rows_and_gaps ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 23 filtered out
```

## Evidence captured

- Static, deterministic rows identify function-like (`make_item`), attribute
  (`traced`), and derive (`derive_builder`) proc-macro exports.
- Static invocation candidates identify `Builder`, `traced`, and `make_item`.
- Every export remains `syntax_only`; the fixture explicitly emits gaps for
  unobserved output token streams and proc-macro file access.
- The test runs capture twice and verifies identical inventories, proving the
  fixture output is deterministic.

Completion gate: proc macros detected. Validation gate: proc-macro fixture
emits static rows and required capture gaps without executing a proc macro.
