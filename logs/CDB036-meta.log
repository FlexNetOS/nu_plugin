# CDB036 meta repo selection validation
2026-07-27T15:47:01Z

## isolated execution

```text
CARGO_TARGET_DIR=/tmp/cdb036-meta.07ArFN/target \
  cargo run --quiet -p codedb -- scan \
  --repo-id meta-cdb036 \
  --repo-path /tmp/cdb036-meta.07ArFN/selected-repo \
  --store /tmp/cdb036-meta.07ArFN/meta-selection.redb \
  --format json
```

## selected repo scan result

```json
{
  "table": "meta_repo_selection",
  "repo_id": "meta-cdb036",
  "repo_path": "/tmp/cdb036-meta.07ArFN/selected-repo",
  "store_path": "/tmp/cdb036-meta.07ArFN/meta-selection.redb",
  "selection_source": "explicit_repo_path",
  "mutation_policy": "read_only_no_meta_mutation"
}
```

The scan completed with one filesystem entry (the selected empty directory),
zero Rust items, and no Cargo manifest. The supplied store path did not exist
after the scan, proving that this read-only selection flow did not create or
mutate a store.

## export selection result

`codedb export meta_repo_selection` returned one Nuon row with the same
`repo_id`, canonical `repo_path`, `store_path`, `selection_source`, and
`mutation_policy` fields.

## conflicting selection refusal

```text
codedb: conflicting repo selection: positional repo path /tmp/cdb036-meta.07ArFN/one differs from explicit repo path /tmp/cdb036-meta.07ArFN/two
```

The command exited nonzero. No meta graph or repository artifact was mutated.

## regression suite

```text
CARGO_TARGET_DIR=/tmp/cdb036-meta.07ArFN/target cargo test --quiet -p codedb
42 + 5 + 5 + 2 + 4 tests passed; 0 failed.

## 2026-07-27 replay verification

```text
CARGO_NET_OFFLINE=true CARGO_TARGET_DIR=/tmp/cdb036-meta.Q37Mz3/target \
  cargo run --quiet -p codedb -- scan \
  --repo-id meta-cdb036-20260727 \
  --repo-path /tmp/cdb036-meta.Q37Mz3/selected-repo \
  --store /tmp/cdb036-meta.Q37Mz3/meta-selection.redb \
  --format json
```

The isolated selected-repository scan succeeded. Its first row identified
`meta-cdb036-20260727`, `selection_source = explicit_repo_path`, and
`mutation_policy = read_only_no_meta_mutation`. A subsequent
`codedb export meta_repo_selection` returned the same selection fields in
Nuon. The supplied store path was absent after both commands
(`STORE_ABSENT=verified`), so this replay neither materialized a CodeDB store
nor mutated any meta-owned graph or repository artifact.
```
