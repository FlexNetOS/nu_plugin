# CDB038 Yazelix placement validation
2026-07-02T06:44:47Z
## required placement statements
7:Yazelix hosts CodeDB as a runtime tool. CodeDB is not a Yazelix plugin, not a
20:not bypass Yazelix pane/session ownership.
## host/runtime Nu distinction
24:Host Nushell and Yazelix runtime Nushell are separate runtimes. They can have
25:different binary paths, versions, plugin protocol compatibility, registry files,
32:| Host Nu | `codedb doctor --nu --format json` reports host `nu` path, version, protocol compatibility, and registration guidance. |
33:| Yazelix runtime Nu | `codedb doctor --yazelix --format json` reports the Yazelix runtime `nu` when discoverable from explicit Yazelix environment variables. |
## no tracked config mutation
40:the operator's real HOME and must not mutate the tracked Yazelix runtime tree.
53:tracked nushell/config/config.nu direct mutation
## placement surfaces
13:- a Nu plugin registered inside Yazelix runtime Nushell;
14:- a CLI executable reachable from Yazelix shells and popups;
15:- a Codex sidecar tool launched inside the Yazelix terminal workflow;
16:- a status/report source if a later Yazelix widget consumes capture status.
## forbidden ownership claims absent
no forbidden Yazelix ownership claims found

## 2026-07-27 deterministic review

Validated `docs/YAZELIX_PLACEMENT.md` against PRD sections 16.3 and 24.

- The document names CodeDB a Yazelix runtime tool and explicitly rejects
  Yazelix-plugin, Zellij-plugin, generated-initializer, and startup-config
  ownership claims.
- It distinguishes host Nushell from Yazelix runtime Nushell by binary path,
  version, plugin protocol, registry, environment, and HOME/XDG root; it also
  assigns separate `codedb doctor --nu` and `codedb doctor --yazelix` checks.
- It keeps CLI fallback available for missing, degraded, or incompatible
  plugin registries.
- It requires temporary HOME/XDG roots for registry smoke tests and forbids
  tracked `nushell/config/config.nu` mutation.

Validation result: `host/runtime Nu distinction documented`.

## 2026-07-28 dry-run review handoff

Reviewed the current placement artifact against the authoritative CDB038 row,
PRD sections 16.3 and 24, and the package checklist's Yazelix bridge gates.
The review used read-only text assertions only; it did not launch Nushell,
register a plugin, alter a HOME/XDG root, or mutate a Yazelix runtime tree.

- `docs/YAZELIX_PLACEMENT.md` names CodeDB a Yazelix runtime tool and explicitly
  rejects Yazelix-plugin and Zellij ownership claims.
- It distinguishes host Nu from Yazelix runtime Nu, including independent paths,
  versions, protocols, registries, environment roots, and `codedb doctor`
  checks.
- It preserves CLI fallback for unavailable or incompatible plugin registries and
  requires temporary HOME/XDG roots for registry smoke tests.
- It confines the generated bridge to Yazelix state/cache/config output paths and
  prohibits direct mutation of tracked `nushell/config/config.nu`.

Deterministic validation result: `host/runtime Nu distinction documented`.
Artifact SHA-256: `c25f0208a3bf051c4d408e548d80e5c5b0bee156bffaba71c2de0556fdd1679c`.
