CDB051 — Host Nu and Yazelix runtime Nu protocol review
Date: 2026-07-28
Status: passed

Scope
- Reviewed `codedb doctor --nu --yazelix` against PRD section 14 and the
  Yazelix runtime-discovery contract.
- No plugin registry, HOME/XDG state, Yazelix configuration, or source
  repository was mutated.

Implementation evidence
- Host Nu is discovered from PATH; Yazelix Nu is discovered independently from
  `YAZELIX_NU_BIN`, `YAZELIX_NU_PATH`, `YAZELIX_RUNTIME_NU`, `YZX_NU`, or
  `YAZELIX_TOOLBIN/nu`.
- Doctor emits path, version, locked protocol compatibility, plugin binary,
  and non-mutating registration-status rows per discovered runtime.
- The locked Nu plugin/protocol handshake is `0.113.1`.
- A missing runtime, version mismatch, or executable that cannot complete
  `nu --version` returns degraded rows rather than aborting the doctor command.

Validation
- `rustfmt --edition 2024 --check crates/codedb/src/main.rs` — passed.
- `cargo test -p codedb doctor_` — passed: 4 tests.
- `cargo test -p codedb --test relative_repo_path` — passed: 4 tests.
- `cargo run -q -p codedb -- doctor --nu --yazelix --format json` — passed.
  The host Nu reported `0.113.1` and compatible protocol status; no Yazelix
  runtime variable was configured, so `yazelix_nu.runtime_nu_path` correctly
  reported `degraded`.

Note
- Workspace-wide `cargo fmt --check` remains blocked by pre-existing,
  out-of-scope formatting drift in `crates/codedb/src/ingest.rs` and
  `crates/nu_plugin_codedb/src/main.rs`; neither file was modified here.
